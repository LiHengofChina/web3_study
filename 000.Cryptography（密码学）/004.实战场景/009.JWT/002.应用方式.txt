
//=================================================================== 应用方式
//=================================================================== 应用方式

JWT 的签名部分可以使用 "对称" 或 "非对称" 密钥进行 "加密"，
因此它可以通过两种方式来保护数据：


（1）基于 "对称密钥" 的 "HMAC（消息认证码）"。
（2）基于 "非对称密钥" 的 "数字签名"。

