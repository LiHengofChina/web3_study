
所谓“证书格式”，确实可以理解为密钥格式的一种扩展表现形式，只是包含了更多信息。


证书格式（如 PKCS12、PEM、DER）本质上就是“密钥或密钥+证书的封装格式”。

	：密钥（Key）	公钥、私钥，本体是二进制数据
	：证书（Certificate）	含有公钥 + 颁发者信息 + 签名 等的结构化数据
	：证书格式 / 密钥格式	是为了保存/传输密钥或证书时使用的一种“编码和封装方式”

