

//================================================== 消息认证码（MAC - Message Authentication Code）
//================================================== 消息认证码（MAC - Message Authentication Code）

（1）MAC 用于 "验证" 消息的 "完整性和真实性"，
		类似于 "签名"，但通常使用 "对称密钥"。
		 "接收者" 和 "发送者" 共享 "相同的密钥"。
		//常见算法：HMAC（基于哈希的MAC），如 HMAC-SHA256。




//================================================== HMAC
//================================================== HMAC
	HMAC 是基于 "哈希" 的MAC 的MAC。



 